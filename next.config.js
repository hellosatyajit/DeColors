/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['juewdrvuynzvupklbxme.supabase.co', 'utfs.io', 'm.media-amazon.com']
    }
};

module.exports = nextConfig;
